import styled from "styled-components";

export const StyledContactsFooter = styled.footer`
  border: 1px solid;
  padding: 1em;
  flex-grow: 1;
  display: flex;
  flex-direction: row;
  justify-content: space-evenly;
`;
